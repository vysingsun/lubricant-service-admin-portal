# Base URL of the lubricant-service-backend NestJS API
NUXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Base URL of kot-preng-api, used directly by the frontend for login
# (the NestJS backend only verifies tokens, it doesn't issue them)
NUXT_PUBLIC_KOT_PRENG_API_URL=http://localhost:3002/api
